what are the variable naming conventions in javascript? 
     variables are case sensitive.
     variables can be simple characters,multiple characters or alpha numeric.
     camel casing.
     only 2 symbols canbe used,they are $ and _ .
     cannot start with a number num1 or 1num.
     cannot use keywords as variables.keyword are reserved words that has pre defined meanig.
     variables cannot have spaces.  

create a greeting alert (use =>prompt,message,alert)
     let name= prompt("enter your name")
     alert("hello "+name)
     alert("good morning")
write some code so that the values of the below variables switch around
let a=5,let b=8.switch the value so that a holds the value 8 and the variable b holds the value 5 
     let a =5
     let b =8
     a=b
     b=a
     console.log(a)
     console.log(b)